#!/bin/bash -x
for file in 'ls *.text';
do
    folderName='echo $abc | awk -F. '{print $1}'';
    
      if [ -d $abc ] ;
       then
       (rm -R abc);
       fi
       
       (mkdir -p $def);
   
       (cp $abc $def);
     done
